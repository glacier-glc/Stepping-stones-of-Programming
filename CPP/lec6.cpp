#include <iostream>
using namespace std;
int main(){ int a=5, b=3;
   cout<<"a + b  "<<(a + b)<<endl; //multifunction cursor "alt " alt shift + drag     
   cout<<"a - b  "<<(a - b)<<endl; //multifunction cursor "alt " alt shift + drag 
   cout<<"a * b  "<<(a * b)<<endl; //multifunction cursor "alt " alt shift + drag 
   cout<<"a / b  "<<(a / b)<<endl; //multifunction cursor "alt " alt shift + drag    
   cout<<"a % b  "<<(a % b)<<endl; //multifunction cursor "alt " alt shift + drag 
   cout<<a++   <<endl;
   cout<<a--<<endl;
   cout<<++a<<endl;
   cout<<--a<<endl;

   cout<<"a == b  "<<(a == b)<<endl; //multifunction cursor "alt " alt shift + drag 
   cout<<"a != b  "<<(a != b)<<endl; //multifunction cursor "alt " alt shift + drag 
   cout<<"a < b  "<<(a < b)<<endl; //multifunction cursor "alt " alt shift + drag 
   cout<<"a > b  "<<(a > b)<<endl; //multifunction cursor "alt " alt shift + drag 
   cout<<"a <= b  "<<(a <= b)<<endl; //multifunction cursor "alt " alt shift + drag 
   cout<<"a >= b  "<<(a >= b)<<endl; //multifunction cursor "alt " alt shift + drag 
   cout<< (a==b) && a<b;
   cout<< (a!=b) || a<b;
   cout<< (a==b) && a<b;
   cout<< (a!=b) && a>b;
   cout<<!(a==b);
   return 0;


    return 0; 
}